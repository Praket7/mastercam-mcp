# Codex configuration

Register the server as a local stdio MCP server using the absolute Node executable and the built `dist/server.js` file. Keep the environment profile at `read` for normal work.

This checkout could not perform a Codex live call because Mastercam and its SDK were not installed during preflight. The mock contract path is covered by automated tests.
